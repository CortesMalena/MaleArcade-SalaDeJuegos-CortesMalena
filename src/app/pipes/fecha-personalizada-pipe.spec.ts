import { FechaPersonalizadaPipe } from './fecha-personalizada-pipe';

describe('FechaPersonalizadaPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaPersonalizadaPipe();
    expect(pipe).toBeTruthy();
  });
});
